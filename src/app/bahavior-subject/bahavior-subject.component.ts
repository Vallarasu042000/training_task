import { Component } from '@angular/core';
import { BehaviorSubject } from 'rxjs';

@Component({
  selector: 'app-bahavior-subject',
  templateUrl: './bahavior-subject.component.html',
  styleUrls: ['./bahavior-subject.component.css']
})
export class BahaviorSubjectComponent {

 // public mySubject = new BehaviorSubject<number>(0);
//  var subject = new Rx.Subject();
// const behaviorSubject = new Rx.BehaviorSubject([]);

  

 //  const mySubject = new Rx.Subject().subscribe((v) => console.log(v));

// mySubject.next(1);
// mySubject.next(2);

// mySubject.subscribe((x) => console.log('The second Subscription : ' + x));

// mySubject.next(3);

}
